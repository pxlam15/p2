<?php

$numWords = $_POST['num']; 
$wordList = Array();

for($i = 0; $i < $numWords; $i++){
	$wordList[$i] = 'hello'.$i;
	echo $wordList[$i];
}

# still need to get random ass words
# still need to do random ass symbols
# still need to do random ass numbas
# need to echo you whore